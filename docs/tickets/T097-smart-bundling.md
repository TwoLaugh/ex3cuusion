# T097 — Smart temptation bundling

Pair passive-attention nourishment with grind tasks via existing overlap
lanes. Only suggestions whose attention profile is passive/background
(old favourite album, audiobook) qualify — never full-focus material
(films, new fiction). Surfaces as a tray pairing: "clear boxes + album?".

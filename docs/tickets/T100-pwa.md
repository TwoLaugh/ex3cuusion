# T100 — PWA (Android ladder, rung 2)

Manifest + icons + service worker (offline-tolerant shell, network-first
API), install prompt. Rung 3 (Capacitor: notifications, widget) only
after the list-first design survives two weeks of real phone use.
Rejected for now: share-target (user), native rewrite (premature).

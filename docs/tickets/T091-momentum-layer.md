# T091 — Momentum layer

User decision (2026-06-10): chosen from the "what excites you" set.

- Streaks for recurring tasks (derive from completions/lastCompletedAt;
  no new write path): flame + count on the task row and Now card.
- Week heat-strip: 7 cells of completion density on the Today header
  (planned vs done per day).
- End-of-day close-out moment: when the last committed item resolves,
  a quiet "day closed — N done, streaks kept" card (feeds the existing
  daily review rather than replacing it).
- Tone: warm, minimal, no gamification noise; accent color only.
